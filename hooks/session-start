#!/usr/bin/env bash
# SessionStart hook for elenchus plugin

set -euo pipefail

# Determine plugin root directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd)"
PLUGIN_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

# Read using-elenchus content
using_elenchus_content=$(cat "${PLUGIN_ROOT}/skills/using-elenchus/SKILL.md" 2>&1 || echo "Error reading using-elenchus skill")

# Escape string for JSON embedding using bash parameter substitution.
escape_for_json() {
    local s="$1"
    s="${s//\\/\\\\}"
    s="${s//\"/\\\"}"
    s="${s//$'\n'/\\n}"
    s="${s//$'\r'/\\r}"
    s="${s//$'\t'/\\t}"
    printf '%s' "$s"
}

using_elenchus_escaped=$(escape_for_json "$using_elenchus_content")
session_context="<EXTREMELY_IMPORTANT>\nYou have elenchus.\n\n**Below is the full content of your 'elenchus:using-elenchus' skill - your introduction to using skills. For all other skills, use the 'Skill' tool:**\n\n${using_elenchus_escaped}\n</EXTREMELY_IMPORTANT>"

# Output context injection as JSON.
# Keep both shapes for compatibility:
# - Cursor hooks expect additional_context.
# - Claude hooks expect hookSpecificOutput.additionalContext.
cat <<EOF
{
  "additional_context": "${session_context}",
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "${session_context}"
  }
}
EOF

exit 0
